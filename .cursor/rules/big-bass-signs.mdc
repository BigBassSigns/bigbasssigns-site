---
description: Live Big Bass Signs site — IA, design, URLs, photos, deploy
alwaysApply: true
---

# Big Bass Signs live site

Repo: `BigBassSigns/bigbasssigns-site` on `main`. Cloudflare Worker `muddy-wind-ead0` deploys from main.
Canonical host that actually serves pages today: `https://www.bigbasssigns.com`.
Apex `https://bigbasssigns.com/*` currently 301s to the www homepage and drops the path — do not assume apex inner URLs work until that Cloudflare rule is fixed.

Chris reviews and directs. Cursor does the work. Do not ask him to copy files, run git commands, or click through Cloudflare.

Edit this static site in place. Do not create a second site or a navy/orange theme.

## Design

- Charcoal header `#1c1c1c`, gold buttons `#c4a046`, white pages, system fonts
- Match `css/styles.css` (mobile-first)
- Nav: Menu checkbox + `<details>` dropdowns (Construction / Business / Vehicles / About + Contact + phone)

## IA (customer type)

- Construction: land use, SWPPP, safety, boards/install, grants, `/Construction-Stages`
- Business: banners, storefront, directional
- Vehicles: lettering, stickers, truck door kits, `/boat-letters`, `/carpet-decals`, `/specials/truck-logo-packages`. Do not push full wraps.
- About: shop, `/home/equipment`, `/service-area`
- Contact: `/contact`

Keep old Google Sites URLs for Ads: `/Sign-Details/*`, `/Construction-Stages/*`, `/service-area/*`, `/home/equipment`. Do not delete old paths; add a 301 in `_redirects` if a URL moves.

Priority SEO city pages only: Lynnwood, Everett, Redmond, Sammamish, Woodinville. Other cities stay under `/service-area`.

## Photos — Cursor owns this

JPGs/PNGs live in `photos/` with stable names. HTML uses `/photos/...` — never `drive.google.com/thumbnail`.
Grok cannot push binary files to GitHub. When photos are missing on the live site, find them on this Mac (Grok artifacts `photos/`, Google Drive Marketing folders, Desktop, Downloads), copy into repo `photos/`, commit, and `git push origin main` without asking Chris.
Needed now if missing: `stickers-01.jpg` `stickers-02.jpg` `stickers-03.jpg` `carpet-01.jpg` `carpet-02.jpg` `carpet-03.jpg` `yard-01.jpg` `yard-02.jpg` `yard-03.jpg`.
Do not use `photos/business-01.jpg` as wrap portfolio (Chris’s truck).
Do not invent jobs, clients, or photos.

## Content

Customer-facing, AdWords-rich, audience-tailored. No prompt language on the site. No house street address. Email `Sales@BigBassSigns.com` first. License `BIGBABS743C4` in the footer only.

## Deploy

You commit and push to `main`. Worker deploys from main. Do not wait for Chris to run git.
